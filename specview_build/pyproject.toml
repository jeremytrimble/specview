[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "specview-build"
version = "0.1.0"
dependencies = [
    "hatchling>=1.18.0",
]

[project.entry-points.hatch]
specview = "specview_build.hooks"